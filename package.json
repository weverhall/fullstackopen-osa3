{
  "name": "fullstackopen-osa3",
  "version": "1.0.0",
  "description": "fso",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ~/vsc-projut/fullstackopen/osa2/puhelinluettelo && npm run build && cp -r build cd ~/vsc-projut/fullstackopen-osa3",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "proxy": "http://localhost:3001",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  }
}
